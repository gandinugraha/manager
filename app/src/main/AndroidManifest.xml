<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mrrobot.manager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BusinessDetailActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".PromoActivity" />
        <activity android:name=".StylishActivity" />
        <activity android:name=".AddNewStylishActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".PromoDetailActivity" />
        <activity android:name=".UserActivity"></activity>
    </application>

</manifest>